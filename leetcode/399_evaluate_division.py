def calcEquation(self, equations: List[List[str]], values: List[float], queries: List[List[str]]) -> List[float]:
    graph = {}
    for index in range(len(equations)):
        temp = graph.get(equation[0])
        temp.append()

if __name__ == '__main__':
    pass